import * as React from 'react';
import Registration from '../../core/Registration/Registration';

const Registrations: React.FC = props => {
  return (
    <div>
      <Registration />
    </div>
  );
};

export default Registrations; 
