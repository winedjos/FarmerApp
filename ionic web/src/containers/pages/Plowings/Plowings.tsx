import * as React from 'react';
import Plowing from '../../core/Plowing/Plowing';

const Plowings: React.FC = props => {
  return (
    <div>
      <Plowing />
    </div>
  );
};

export default Plowings; 