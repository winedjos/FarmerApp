import * as React from 'react';
import WeedRemove from '../../core/WeedRemove/WeedRemove';


const WeedRemoves: React.FC = props => {
  return (
    <div>
      <WeedRemove />
    </div>
  );
};

export default WeedRemoves; 