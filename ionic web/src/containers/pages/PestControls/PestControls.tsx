import * as React from 'react';
import PestControl from '../../core/PestControl/PestControl';

const PestControls: React.FC = props => {
  return (
    <div>
      <PestControl />
    </div>
  );
};

export default PestControls; 